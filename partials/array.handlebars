/* START {{array.name}} arrays */
{{array.type}}[] public {{array.name}}s;

function get{{array.name}}sLength() constant returns (uint) {
  return {{array.name}}s.length;
}

function add{{array.name}}({{array.type}} value) requireOwnership {
  {{array.name}}s.push(value));
}

function get{{array.name}}(uint index) returns ({{array.type}}) {
  return {{array.name}}s[index];
}
/* END {{array.name}} arrays */
